<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only. Points the app at the cleartext allowance for the local token server; a release build
  never sees this file, so it ships with the platform default of cleartext denied.

  The config names two addresses, so cleartext is permitted to those and to nothing else. It is
  ignored below API 24, which is why the module's minSdk is 24 rather than the 23 the SDK modules
  build against: a 23 build would have no config at all and would permit cleartext outright.

  allowBackup is repeated from the main manifest rather than inherited. The merged value is the same
  either way; stating it here keeps the file readable on its own, since a reader looking at what the
  debug variant permits should not have to open a second file to learn what it stores.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
