<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The window background only. Everything the app draws is themed in Compose; this exists so the
      cold-start window and the area behind the composition match the scheme, which Compose cannot
      reach. Kept in step with `surface` in Color.kt: light is Neutral 8, dark is Deep Blue.
    -->
    <color name="window_background">#F9F9FF</color>
</resources>
