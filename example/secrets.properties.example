# Copy to secrets.properties and fill in. secrets.properties is gitignored.
#
# Do not put a client secret here. A partner's client secret must never ship inside a mobile binary.
# Run example-server instead: it performs the exchange and hands this app a short-lived token.
# See example-server/README.md.
#
# Every setting below can also be passed for a single run without editing this file:
#   ./gradlew :example:installDebug -Ppayabli.demo.entryPoint=test6
#
# Or from the environment, which is what CI and an already-exported shell use. The variable is the setting
# uppercased with dots as underscores, and the -P flag wins over it:
#   PAYABLI_DEMO_ENTRYPOINT=test6 ./gradlew :example:installWithTelemetryDebug
#
# The two groups below are different things. The first is what Payabli has to know about you, and a real
# integration sets the same four. The second is how this sample reaches its own token server and what it
# draws; none of it exists in an integration, because a partner's own backend mints the token.


# ---------------------------------------------------------------------------
# Required by Payabli
# ---------------------------------------------------------------------------

# The paypoint. The only setting with no working default: without it nothing can be sent, and every
# screen says so.
payabli.demo.entryPoint=

# Which Payabli environment this build talks to: sandbox or production. An entry point exists in one of
# them, so this and the entry point above are always set together. Blank means sandbox.
payabli.demo.environment=

# This app's package name. The tap-to-pay readiness check compares it against the package actually
# running and against this binary's signing certificate, so a mismatch shows up on screen instead of
# being rejected much later by attestation.
payabli.demo.appId=com.payabli.example.app

# SHA-256 of the certificate this app is signed with, as the Play Console shows it. Set it and the
# tap-to-pay readiness check compares the running build against it, so a build signed by the wrong
# key is caught here rather than by attestation much later. Leave it blank and the check says the
# signing key was not verified.
payabli.demo.signingCertificate=


# ---------------------------------------------------------------------------
# Needed by this sample only
# ---------------------------------------------------------------------------

# Where the token server is. Blank resolves per run: an emulator reaches this machine at 10.0.2.2,
# and a physical device reaches it at 127.0.0.1 once `adb reverse tcp:8787 tcp:8787` is set up.
# Set a bare host, host:port, or a full URL to pin it, which is what a LAN address needs.
payabli.demo.tokenHost=

# Where the local token server is reached when nothing overrides it. Change these when the emulator
# maps the host machine differently, or when adb reverse forwards another port. The defaults are the
# standard ones: 10.0.2.2 is the emulator's alias for the host machine's loopback interface, and
# 127.0.0.1 is the device's own loopback, which adb reverse forwards to the development machine.
payabli.demo.emulatorTokenHost=
payabli.demo.deviceTokenHost=
payabli.demo.tokenPort=

# Redacted request and response logging on the payment screens.
payabli.demo.diagnostics=true

# Fills the payment forms with values that pass, so a walk through the flows needs no typing.
payabli.demo.prefill=false
